<%= render(ModalComponent.new(title: @event.title)) do %>
  <% if @event.recurring.present? %>
    <div class="mb-16">
      <%= render "recurring_dropdowns", event: @event %>
    </div>
  <% else %>
    <%= button_to "Edit", edit_event_path(@event), method: :get, class: "btn btn-primary" %>
    <%= button_to "Destroy", @event, method: :delete, data: { turbo_frame: '_top' }, class: "btn btn-error" %>
  <% end %>

  <ul>
    <li>Start: <%= @event.starts_at %></li>
    <li>End: <%= @event.ends_at %></li>
    <li><div class="badge badge-outline" style="border-color:<%= @event.color %>; color:<%= @event.color %>">Color</div></li>
  </ul>
<% end %>
